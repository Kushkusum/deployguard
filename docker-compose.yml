services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  orders:
    build: ./services/orders
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - BUG_MODE=false
    depends_on:
      - redis